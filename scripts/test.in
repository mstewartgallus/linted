#! /usr/bin/env sh

set -e
set -C
set -u

SRCDIR="@srcdir@"
ROOT="$(cd "$(dirname "$0")" && pwd)"

exec "${ROOT}/../src/init/linted"\
     --fstab="${SRCDIR}/src/init/fstab"\
     --simulator="${ROOT}/../src/simulator/simulator"\
     --gui="${ROOT}/../src/gui/gui" "$@"
