#! /usr/bin/env sh

set -e
set -C
set -u

ROOT="$(cd "$(dirname "$0")" && pwd)"

exec "${ROOT}/../src/init/linted" --simulator="${ROOT}/../src/simulator/simulator" --gui="${ROOT}/../src/gui/gui" "$@"
